/**
 * Intelligence + automation schema lives only in the committed Prisma
 * migration `prisma/migrations/20260924120000_bsos_intelligence_automation`.
 *
 * Preview shares Production and skips migrate. This module does not run
 * ALTER/CREATE/index/constraint SQL on authenticated requests.
 */
export const INTELLIGENCE_SCHEMA_SOURCE = "prisma-migrate" as const;
